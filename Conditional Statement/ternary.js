// ternary operators
// formula :let result = a?a:b; 
// it's means :condition ? output true : output false;
// it's a compact if else statement
let age = 22;
let result = age >= 18 ? "Adult" : "Not Adult";

// age >= 18 ? console.log("Adult") : console.log("Not Adult");

console.log(result);
